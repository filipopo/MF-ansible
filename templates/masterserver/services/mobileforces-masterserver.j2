[Unit]
Description=Mobile Forces master server
After=network.target

[Service]
User={{ server_user }}
WorkingDirectory={{ masterserver_path }}/bin/
ExecStart={{ masterserver_path }}/bin/MasterServer-Qt5
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target
