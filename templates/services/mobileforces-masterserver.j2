[Unit]
Description=Mobile Forces master server
After=network.target

[Service]
User={{ server_user }}
WorkingDirectory={{ game_path }}/System/
ExecStart=sh {{ game_path }}/System/RunMasterServer.sh
Restart=on-failure

[Install]
WantedBy=multi-user.target
